# The Play Billing Library and Tauri ship their own consumer ProGuard
# rules, so there is nothing plugin-specific to keep here. File exists
# because build.gradle.kts references it (consumerProguardFiles).
